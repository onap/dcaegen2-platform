CREATE TABLE IF NOT EXISTS dti.rt_l3_network
(
    NETWORK_ID                VARCHAR(100) NOT NULL,
    NETWORK_NAME              VARCHAR(150),
    NETWORK_TYPE              VARCHAR(40),
    NETWORK_ROLE              VARCHAR(100),
    NETWORK_TECHNOLOGY        VARCHAR(50),
    NEUTRON_NETWORK_ID        VARCHAR(100),
    IS_BOUND_TO_VPN           VARCHAR(1),
    SERVICE_ID                VARCHAR(150),
    NETWORK_ROLE_INSTANCE     DECIMAL(32),
    RESOURCE_VERSION          VARCHAR(25),
    ORCHESTRATION_STATUS      VARCHAR(25),
    HEAT_STACK_ID             VARCHAR(150),
    MSO_CATALOG_KEY           VARCHAR(200),
    CONTRAIL_NETWORK_FQDN     VARCHAR(200),
    MODEL_INVARIANT_id        VARCHAR(150),
    MODEL_VERSION_ID          VARCHAR(100),
    MODEL_CUSTOMIZATION_ID    VARCHAR(150),
    WIDGET_MODEL_ID           VARCHAR(150),
    WIDGET_MODEL_VERSION      VARCHAR(25),
    PHYSICAL_NETWORK_NAME     VARCHAR(40),
    IS_PROVIDER_NETWORK       VARCHAR(1),
    IS_SHARED_NETWORK         VARCHAR(1),
    IS_EXTERNAL_NETWORK       VARCHAR(1),
    OPERATIONAL_STATUS        VARCHAR(25),
    SELFLINK                  VARCHAR(4000),
    UPDATED_ON                VARCHAR(20),
    PRIMARY KEY (NETWORK_ID)
);
